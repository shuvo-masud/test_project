#Apple Orange Classifier

The model is trained on a labeled dataset containing images of apples and oranges. It learns visual features such as shape, color, and texture using a CNN architectur

key Features
CNN-based deep learning model built with TensorFlow/Keras
Achieves 92% classification accuracy

model saving and reuse (.keras format)
Image preprocessing with resizing and normalization
Feature extraction for similarity-based decision making
